# City Builder TODO

Running list of things to do. The user (Atlas) adds items; we work through them
together. Move completed items to **Done** with a date so the history is
visible without needing `git log`.

Order is build-order: small fixes first, foundations next, economy retuning
after that, polish, then big design lifts last.

---

## Up next

- [ ] **Safari pinch-zoom bug** — on the other player's iPhone, after zooming in they can't zoom back out, and walkers shift to a different position (possibly resize too). Investigate Safari-specific gesture handling, walker positioning math under zoom, and visualViewport / transform-origin gotchas. Adding zoom buttons (next item) may sidestep this entirely.
- [ ] **Zoom in/out buttons on the map UI.** Desktop and mobile. Wire through the existing `setMapZoom` path so behavior matches scroll/pinch zoom. May also serve as a workaround for the Safari bug above.
- [ ] **Animations Pass 2 polish (post-deploy review).** Pass 1 + Pass 2 shipped 2026-05-05. After Atlas looks at the deployed result, fine-tune any animations that feel off and pick up the buildings I left on the generic worker bob (foresters_office / foreman_office / clay_master_hut / mine_office, mosaic_workshop, engineer_workshop, distillery, brewery beyond steam, cabinetmaker beyond worker).
- [ ] **Recurring scheduled trades between players.** Today's player-trade is one-shot. Add an "agreement" object that fires on an interval as long as both sides have the resources required. Needs schema (parties, resources/qtys/direction, interval, last_fired_at, both-side acceptance), a server-side resolver (lazy/cron-style like `resolve_trader_visit`), UI to propose / accept / list / cancel agreements, and integration with the existing player-trade infrastructure. Larger design task — Plan + Atlas review before code.
- [ ] **Inspector.js split (paused).** 912-line file with three modes (building / resource tile / walker) sharing module-private state and the same overlay DOM. Clean split needs an "inspector core" state-machine module with three "mode" plug-ins — structural redesign, not a mechanical move. Resume during a session where Atlas is at the browser. See `~/.claude/projects/-home-atlas-citybuilder/memory/project_pending_refactors.md`.
- [ ] **Rebaseline `baseline_schema.sql` (paused).** 38+ migration patches stacked since the last baseline. Needs `pg_dump` installed locally (`! sudo apt install -y postgresql-client` from the chat prompt). Once installed: dump live schema → replace baseline → archive the patches → run tests → push.
- [ ] **Happiness system: citizens immigrate when happy, emigrate when unhappy.**
  - **Concept:** a per-player happiness rating (likely 0–100, an average across all citizens) that drives the *rate* at which citizens enter or leave the city — i.e. shifts the worker pool over time, not all-at-once.
  - **Mechanism (sketch):** at each tick, compute happiness, then nudge worker pool: positive nudge if happy, negative if unhappy. Over enough time, a happy city grows past its housing capacity (forcing the player to build more); an unhappy one shrinks back below it. Could also gate housing tier evolution.
  - **Inputs to weigh:** housing tier (higher = happier), service access (well, school, temple, bathhouse, tavern within range), tax pressure (Tax Office in range = unhappy), food variety (more food types in stock = happier), density (overcrowding = unhappy), road access, unstaffed buildings nearby, etc.
  - **Effects (decide between):** (a) directly modifies worker pool (immigration/emigration), (b) modifies housing growth rate, (c) modifies extractor/processor productivity, (d) some combination. Atlas's framing favors (a) — explicit population shifts over time.
  - **Design discussion needed** before any code lands. Open questions: is happiness per-house or per-player? per-house lets us show heatmap on the map; per-player is simpler. How fast should the worker pool actually shift? What's the equilibrium point?

---

## Done

- **2026-05-05** — Trade progression overhaul: NPCs no longer auto-available at start; player-to-player trade is the only option until the unlock gate is met (≥1 active extractor + ≥1 active food extractor + ≥1 tier-1 house). Once unlocked, the city's NPC trader pool is accessible — *city* = all districts in the world combined, *district* = one player. Each trader periodically posts an open city-wide request; players donate from inventory, faster fulfillment + larger contribution = more reputation. Personal reputation per (player, trader) feeds into a city reputation that's a population-weighted average across all districts. City rep tiers (50/150/400/1000) gate trader inventory expansion. Reputation decays 2%/day if no donations in the last 7 days. Black market sell prices cut by ~35% so the gate has tangible value. Trade tab restructured into Partners / Missions / Players / Stats sub-tabs (top-level Players tab dropped). Locked-state UI shows the three requirements with check marks. Stats sub-tab summarizes today/week/all-time imports/exports/balance and top partners. Schema: trader_relationships, trader_missions (unique partial index forces ≤1 open per trader), trader_mission_donations. New RPCs: is_trade_unlocked, district_weight, city_reputation, city_rep_tier, roll_trader_missions, expire_old_missions, donate_to_mission, decay_reputations, get_active_missions, get_trade_partner_view, get_trade_stats. 8 new regression tests; 269 / 269 passing. Design captured in docs/TRADE_PROGRESSION.md.
- **2026-05-05** — Build panel: split Industry section into Industry + Farming. Resource chain (extractor / non-food processors / cross-converters / T4 cross-recipes / resource boosters) stays in `<Industry>`; food chain (food extractor / food-output processors / luxury food / food boosters) moves to a new `Farming` section. Routing distinguishes food vs non-food via the output resource's `is_food` flag and the booster's `boost_target`.
- **2026-05-05** — Build panel: collapsible sections — Infrastructure (road + house), <Industry> Industry (industry-locked production), Civic & Services (services + tax). Replaces a flat 15+ item list. Sections sort by category + tier within; collapse state persists per-section in localStorage so the panel layout survives reloads.
- **2026-05-05** — Toast z-index lifted to 300 so notifications paint above the trade dialog (200) and inspector overlay (150). Send-Offer validation messages were getting hidden behind the dialog they referred to.
- **2026-05-05** — Trade UX overhaul: offer-card wording is now viewer-perspective ("You'd give:" / "You'd receive:" + "From X" / "To X" header) for both inbox and outbox; inbox pre-checks money + inventory and disables Accept with a "Can't accept: missing 5 lumber" notice when you can't fulfill; new `get_player_trade_view` RPC + dialog wiring shows the counterparty's tradeable inventory ("they have N" annotations + dropdown filtered to what they actually hold) and the Send button refuses to ask for more than they have.
- **2026-05-05** — Persist map view (scroll + zoom) per player to localStorage, restored on game reopen so you land where you were last looking instead of at (0,0). Falls back to centering on the player's home tile for a first-ever load. Saved zoom is clamped against the current dynamic minimum so a stored zoom from a smaller district adapts gracefully.
- **2026-05-05** — Building animation audit + Pass 1 + Pass 2. Audit confirmed the issue-suppression contract was already correct (`map.js` adds `.producing` only when staffed + road-connected + fed + unpaused; `.idle::before/::after { animation: none !important }` kills any pseudo-element animation when off). 32 buildings had no `.producing` animation; Pass 1 filled all of them with the established `act-bob` (worker overlay) and `act-smoke` (chimney puff) patterns, tinted per-building (iron worker dark, lime smoke white, glassworks smoke gold, etc.). Pass 2 layered distinctive motion on 11: lantern glow at iron mine, forge glow under smelter & glassworks, sparks off nail forge, a bee weaving across apiary, a fast-pecking chick at hatchery, water shimmer flowing in irrigation channel, water ripple at fishing pier line, full-building sway for orchard / garden / winery. New keyframes: act-glow, act-buzz, act-flow, act-sparks, act-ripple. Multi-tile buildings (tax_man, brewery) wrap position/size in `--bldg-fig-*` so figures stay 1×1-equivalent.
- **2026-05-05** — Trade-offer label fix (outbox view). `renderOfferCard`'s outbox path inverted "You give" / "They give" verbs while also swapping the underlying data, so an offer you sent showed up with the labels reversed. Rewrote the mapping with the sender-perspective DB contract spelled out and inlined the static labels. Inbox already rendered correctly and is unchanged.
- **2026-05-05** — Extract `js/map_roads.js` from `map.js` (1135 → 890 lines). Road autotile SVG generator + placement-time `roadTileSet` cache + connectivity helpers (`getRoadTileSVG`, `rebuildPlacementRoadSet`, `isRoadPlacementConnected`, `roadNeighborFlags`) now self-contained behind a small surface area. Walker module's road graph stays in `walkers.js` (different concern: pathfinding vs. placement validation). Updated `STRUCTURE.md` + the `index.html` cache-buster module list. Removed unused `isRoadAdjacent` while moving.
- **2026-05-05** — `place_building` worker-math drift fix. The function's inline `workers_needed` query was missing `food_extractor` + `booster` categories and didn't require road access for extractors, so the response immediately after placing a building under-reported worker usage until the next `process_production` tick corrected it. Replaced with a shared `_pp_workers_needed(uid)` helper that mirrors `_pp_staff_buildings`'s rules — they can no longer drift. Added regression test that places an orchard then a timber camp and asserts `workers_needed` includes both.
- **2026-05-05** — Refactor `resolve_trader_visit` (212 lines) into orchestrator + 2 phase helpers (`_rtv_sell_phase`, `_rtv_buy_phase`). Same architectural pattern as `process_production`: thin orchestrator over named phases. Each phase independently testable; the orchestrator handles validation, the catch-up loop, money settlement between phases, and visit-row recording. 260/260 tests passing post-refactor.
- **2026-05-05** — Refactor `process_production` (470 lines) into orchestrator + 11 phase helpers (`_pp_compute_worker_supply`, `_pp_housing_supply`, `_pp_tavern_bonus`, `_pp_workers_needed`, `_pp_staff_buildings`, `_pp_run_extractors`, `_pp_run_food_extractors`, `_pp_bump_boosters`, `_pp_run_processors`, `_pp_run_services`, `_pp_run_tax`, `_pp_drain_housing_food`, `_pp_evolve_housing`). Tavern bonus is now captured ONCE at the start of the tick — re-evaluating after the service loop would always succeed because `last_processed_at` gets updated mid-tick (zero elapsed → zero need → trivially met). Establishes the `_pp_*` / `_rtv_*` prefix convention for future PL/pgSQL refactors. 260/260 tests passing.
- **2026-05-04** — Multi-tile buildings + walker variety. Two changes shipped together. **Multi-tile**: `building_types.footprint_w / footprint_h` (default 1×1, max 4×4). `place_building` validates and claims every tile in the footprint; `map_tiles.occupied_building_id` UNIQUE constraint dropped (multiple tiles can share); demolish needs no extra code (FK ON DELETE SET NULL cascades). Frontend renders the anchor's .bldg with absolute positioning + 200%/300% sizes spanning the footprint; interior tiles route clicks back to the anchor via `data-anchor-x/y`. First multi-tile picks: tax_man (2×2), school (2×2), temple (2×2), brewery (2×1). **Walker variety**: WALKER_MAX_COUNT now scales `min(80, 8 + 0.6 × eligible_spawn_count)` so big cities feel populated. Citizen ambient walkers pick a weighted persona — child / elder+cane / couple / pet-walker / merchant / caped / umbrella / hatted — for visual character without 30 sprite variants. New CSS for 3 sprite variants (walker-child / walker-elder / walker-couple) and 5 overlay classes (has-cane / has-pet / has-pack / has-cape / has-umbrella). Tests: 10 multi-tile cases + 10 housing-tier tests + 1 luxury-chain test updated for the larger footprints. 234/234 passing.
- **2026-05-04** — Food tiles. Food extractors (Orchard / Fishing Pier / Garden / Grain Farm) must now be placed *on* a matching food tile (orchard_grove / pond / garden_plot / farmland). Adds 4 new `kind='terrain'` rows to `resources` (not tradeable goods, just FK targets for `map_tiles.resource_node_key`); a `placement_resource_node_key` column on `building_types`; an updated `reject_build_on_resource` trigger that enforces the positive requirement *and* the existing block on every other building. Tile is NOT consumed at placement — demolish reveals it again. `clear_resource_tile` additionally rejects occupied tiles. Cluster seeding extracted into a shared `_seed_resource_clusters` helper; `allocate_district_chunk` now drops 4 ore clusters (6-15 walks each, unchanged) plus 2 food clusters (4-8 walks each). Industry-locked food tiles match ore — a stone player only gets ponds. Existing chunks back-filled by the migration's DO block. New SVG sprites (pond with reeds + lily pads, plowed farmland with stake, walled garden plot with vegetable rows, orchard grove with apple/golden fruit). Inspector adds "Build here" row + terrain-aware copy. Catalog description shows the matching tile requirement. Existing food-extractor tests updated via a new `stamp_food_tile` fixture; choose_industry test bound bumped to accommodate the now-larger cluster variance. 20 new tests; 224/224 passing.
- **2026-05-04** — More housing tiers (6/7/8 — Mansion / Estate / Palace). Caps now extend to t8, with workers ladder 50/70/100 (50/70/100 worker yield; up from 34 at t5). Three new prereq columns on `housing_tier_config`: `needs_luxury_food` (t6+), `needs_industrial_luxury` (t7+), `needs_all_industrial_luxuries` (t8 only). Resource flags `is_luxury_food` (spirits/caviar/spices/ale) and `is_industrial_luxury` (cabinets/monuments/mosaics/machinery). The "all 4" gate is a count comparison (in-stock ≥ total defined) so it generalizes if more industrial luxuries are added later. T8 effectively requires a fully-traded city across all four industries. Inspector blockers + descriptions wired; new SVG sprites for t6/t7/t8 (cupola, walled compound + spired towers, central dome + columned portico). 13 new tests; 204 / 204 passing. Booleans-per-prereq scheme is now at 8 columns — flagged in CLAUDE-time-future as JSON-over-columns once we exceed ~10.
- **2026-05-04** — Phase C2: Advanced cross-recipe T4 buildings. Closes the strategic-depth expansion arc. Each industry has a T4 processor consuming its own T3 output PLUS another industry's cross-good — round-robin: timber needs stone's lime, stone needs clay's glass, clay needs iron's nails, iron needs timber's charcoal. Cabinetmaker (cabinets), Architect (monuments), Mosaic Workshop (mosaics), Engineer's Workshop (machinery). $700/10w, 0.5+0.5 → 0.25. The trade-pressure unlock: every industry's top-tier production now genuinely requires another industry's good. Side-fix: tier CHECK widened from {1,2,3} to {1,2,3,4}. 19 new tests; 191 / 191 passing.
- **2026-05-04** — Phase C: cross-converters + multi-input processor support. 4 new buildings (Charcoal Kiln, Lime Kiln, Glassworks, Nail Forge) producing 4 new support goods (charcoal, lime, glass, nails). Each industry has one cross-converter producing its unique cross-good. The processor loop in `process_production` was extended to support a 2nd input — output is gated by `min(avail/need) across all inputs`, both inputs drain proportional to that progress. Existing single-input processors unchanged. Phase C2 (advanced cross-recipe T4 buildings) split into a follow-up so this commit stayed focused. 17 new tests; 172 / 172 passing.
- **2026-05-04** — Luxury T3 chain extensions (Phase B of the strategic-depth expansion). Each industry's T2 food upgrades into a luxury good: Distillery (wine→spirits), Curing House (smoked_fish→caviar), Spicery (preserves→spices), Brewery (flour→ale). $500/10w/0.5→0.25 each. All four luxury outputs are `is_food=true` so they satisfy the housing food gate; their real strategic value is as high-margin trade goods and the natural prereq for future high-tier housing. Brewery shares flour with Bakery — player chooses (via priority/pause) between commodity bread and luxury ale. 14 new tests; 155 / 155 passing.
- **2026-05-04** — Production boosters (Phase A of the strategic-depth expansion). Each industry has a resource booster + a food booster, all `category='booster'` with new columns `boost_target` ('extractor'/'food_extractor'), `boost_multiplier` (default 1.25), `boost_range` (default 2). Staffed boosters apply MAX multiplier (no stacking) to matching adjacent buildings within range. 8 buildings: Forester's Office, Apiary (timber); Foreman's Office, Hatchery (stone); Clay Master's Hut, Compost Heap (clay); Mine Office, Irrigation Channel (iron). $200 / 3 workers each. 11 new tests. 141 / 141 passing.
- **2026-05-04** — Industry tree buildout. Two-category architecture (per Atlas's framing): industry/food trees are per-industry; housing / road / services / tax are general. Locked existing chain buildings (clay_pit, pottery_kiln → clay; mill, bakery → iron). Filled the tree-shape gaps with 6 new buildings: Smelter (iron T2: iron → iron_ingot), Toolmaker (iron T3: iron_ingot → tools), Tile Maker (clay T3: pottery → tiles), Winery (timber food T2: berries → wine), Smokehouse (stone food T2: fish → smoked_fish), Cannery (clay food T2: vegetables → preserves). 6 new resources matching. Wine, smoked_fish, preserves all `is_food=true` → auto-satisfy the housing food gate. Result: every industry has the same tree shape (T1 extractor / T2 processor / T3 processor + T1 food / T2 food), just with different resources. Cross-conversion (multi-input recipes that take resources from another industry's chain) deferred as a follow-up. 24 new tests; 130 / 130 passing.
- **2026-05-04** — Iron replaces grain as the 4th industry. Grain is now a food only (it always was `is_food = true`; just no longer one of the choosable industries). Iron has its own primary extractor (`iron_mine`, tile-based like the others) and uses `grain_farm` as its paired food extractor (flat-rate, locked to industry='iron'). Atlas's existing grain_farm grandfathered in (still produces; he just can't build new ones). Industry-pick UI now shows all four cards (was missing iron and clay). 7 new tests; 106 / 106 passing.
- **2026-05-04** — Paired food extractors. Each industry has a flat-rate food extractor: **Orchard** (timber → berries), **Fishing Pier** (stone → fish), **Garden** (clay → vegetables); grain industry already had `grain_farm`. New `food_extractor` category in `building_types`; new flat-rate production loop in `process_production` that mirrors the extractor loop minus the path math. Food extractors don't need road access (like extractors). All three new resources marked `is_food = true` so they auto-satisfy the housing food gate. 9 new tests; 98 / 98 passing.
- **2026-05-04** — Tavern bonus is no longer decorative. `v_tavern_bonus` is now pre-computed BEFORE the staffing loop based on each tavern's `(active + road + inputs cover the elapsed tick)` qualification, then added to `v_worker_supply` so the bonus actually buys staffing slots. Net effect: a fed tavern (cost 5, bonus 10) gives a real net +5 staffable workers. Edge case noted in the migration: a tavern that qualifies on inputs but doesn't make it into `v_staffed_ids` would over-count by 10 — rare in practice, accepted vs. an iterative staffing solver. Two new regression tests pin the fix: one shows fed tavern lets an extractor staff that wouldn't otherwise; one shows unfed tavern correctly doesn't pre-boost.
- **2026-05-04** — Housing food consumption per tick. Tier 1+ houses drain food at 0.03 / 0.06 / 0.10 / 0.15 / 0.25 food/min/house (config in `housing_tier_config.food_per_minute`). Drain proportional across all `is_food` resources via single multiplier — no one food unfairly drained first. Player-level `last_food_tick_at` tracks elapsed (separate from per-house `last_processed_at` so the upgrade-secs timer keeps working). When food runs out, the existing devolve gate fires (unless a fed bathhouse is in range). Inventory panel rate display shows the food drain split across foods proportionally.
- **2026-05-04** — Staffing priority + pause. Each worker-consuming building has `staffing_priority` (Low/Normal/High, default Normal); inspector exposes pills + a Pause toggle. Server staffing loop sorts `priority DESC, created_at ASC`. Paused buildings drop out of every `process_production` loop via the existing `status='active'` filter — no per-loop change needed; resume resets `last_processed_at` so paused time isn't dumped into production. Side-fix: `next_starter_row` was picking `max(reserved_row)+1` without checking whether the corresponding `(0, row)` chunk had been claimed by another player's expansion — Atlas had expanded into row 1, breaking new-player allocation. Now walks forward to the first row whose `(0, row)` is unoccupied.
- **2026-05-04** — Housing food gate. Tier 1+ requires "any food" in inventory (presence-only check, not consumption — switch to per-tick drain after the food-pairing item lands so non-grain players have a local food source). Foods flagged via `resources.is_food`; today grain, flour, bread qualify. Tier 0 shanty stays free as the subsistence floor. Devolve fires on starvation unless a fed bathhouse is in range. 6 new tests; 4 existing tier-1+ housing tests stocked grain to isolate their original concerns.
- **2026-05-04** — Inspector "Issues" section. Replaces scattered Road/Workers/Status rows with one consolidated list — every reason a building isn't operational, each with a fix hint. Status row above shows "Operational" or "N issues". Housing upgrade gating now mirrors all server prereqs (road, well, food, school, temple) so the "Next: Conditions met" message is honest. Also fixed `computeRoadAccess` to track service/tax road access (was silently treating them as no-road).
- **2026-05-04** — Service walkers + staffed/unstaffed visual states. Each new service (tavern, bathhouse, school, temple) spawns its own walker sprite when staffed, and the on-map tile shows a producing animation only when staffed + road-connected. Faded sprite + red `!` when unstaffed.
- **2026-05-04** — Citizen-service buildings (×4), each consuming a unique pair of resources to push trade. **Tavern** (bread+pottery → +10 worker capacity, $350/5w/tier 2), **Bathhouse** (brick+clay → blocks housing devolve in 4-tile radius, $300/5w/tier 2), **School** (lumber+flour → gates Townhouse tier 3 in 5-tile radius, $600/10w), **Temple** (statuary+brick → gates Villa tier 4 in 6-tile radius, $800/10w). All require staffing AND ongoing input feeding to operate — drop either input → effect disappears next tick. Schema: added `input_resource_key_2`/`input_rate_2` to `building_types` (cheaper than a join table); added `needs_school`/`needs_temple` to `housing_tier_config`. Process: `process_production` now feeds services in a fourth loop, populates `v_operating_services`, and `v_has_school`/`v_has_temple`/`v_has_bathhouse` queries that set in the housing loop. Tavern's worker bonus uses the existing `output_rate` column (10). Inline-SVG sprites in the kiln style for all four. Tests: 7 covering multi-input charging, partial-feed idle, worker-capacity bonus on/off, tier 3 gate fed/unfed, devolve block fed/unfed.
- **2026-05-04** — District expansion: row-based starters + player-picked expansion. Each player reserves one row at signup; expansion candidates are unowned chunks orthogonally adjacent to the player's district excluding other players' reserved rows. Tap **+ Expand**, candidate chunks pulse gold, tap one to claim. Trapped state impossible because your own row's edges are always available.
- **2026-05-04** — Resource tile rules: can't build on a resource tile (BEFORE INSERT trigger). Tap an owned resource tile to clear it (sets `resource_node_key = NULL`); then it's buildable. Free for now; demolish blocked if an extractor still targets the tile. Tests, including a `clear_resources` shared fixture for tests that build at known coords.
- **2026-05-04** — Wider zoom-out as the map grows. New `computeMinZoom()` returns the smaller of `MAP_MIN_ZOOM` (0.5, the existing static floor for small maps) and the dynamic "fit the whole grid in the viewport" zoom. Hard floor at 0.05 so tiles don't disappear on very large districts.
- **2026-05-04** — Hide tile coordinates in inspector panels. Stripped the `(x, y)` row from buildings, walkers, and the new resource-tile inspector. Walker also lost its redundant duplicate-source row in the cleanup.
- **2026-05-04** — Resource tile inspector. Tapping an owned resource tile now opens the same panel chrome the building inspector uses, with rows listing the deposit's role: harvested by which extractor, processed by which processor → output, optional second downstream step, and the tile coords. Demolish button at the bottom calls `clear_resource_tile` (matches the server's "blocked while an extractor is targeting this tile" rule with an inline warning). The old confirm dialog flow is gone.
- **2026-05-04** — Well polished: 3 workers + road requirement + walker. Staffing flows through the same loop as processors/tax (`category='service'` joined). New 64×64 inline-SVG art in the kiln/mill style — stone ring, water inside, two posts holding a small thatched roof, a bucket on a rope. Well attendants render as plain `citizen` walkers for now. Tax Office still uses the placeholder solid-color tile (its own SVG polish is a separate task).
- **2026-05-04** — Two new buildings: **Well** (`category='service'`, $50, 0 workers) is now a precondition for housing to evolve past tier 0 — needs to be within Manhattan distance 4 of the house. **Tax Office** (`category='tax'`, $300, 10 workers, needs road) credits $10/min when fully staffed via a new section in `process_production`. Placeholder solid-color tile art with letter labels (W / TX); SVG polish later. Happiness hookup deferred to that item.
- **2026-05-04** — Walker variety: WALKER_MAX_COUNT 12→7, spawn chance 0.4→0.22, max steps 18→14, cooldown 4→6 ticks. Per-walker visual jitter via CSS vars: `--wk-scale` (0.85–1.15), `--wk-hue` (±18°, hue-rotates the sprite), `--wk-bob-ms`/`--wk-waddle-ms` (small per-walker animation period jitter). ~18% of ambient walkers spawn `has-hat` → small brown-cap SVG anchored to the head. Same five base sprites, but every walker reads as a slightly different person.
- **2026-05-04** — Worker requirements bumped: extractors and processors all 10 workers each (was 2-4). The all-or-nothing per-building staffing gate already existed in `process_production` (a building only joins `v_staffed_ids` if remaining workers ≥ its worker_cost; only staffed buildings produce). The gate just rarely fired at the old low costs. Now early game is meaningfully tighter — likely future tuning on housing yields if the first-extractor wait feels too long.
- **2026-05-04** — Highway network + remove HQ tile. Every chunk gets a horizontal strip at y_offset=7 and a vertical strip at x_offset=7 stamped as `terrain_type='highway'`, unbuildable, no resource. Highway counts as cost-1 walkable for walker pathing (any owner) and as road-adjacent for `has_road_access`. Roads now connect to highway OR another of your roads (drops the old "adjacent to home" seed rule). City-center special tile removed entirely; `home_x/home_y` retained as logical anchor.
